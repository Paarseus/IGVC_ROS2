diff --git a/include/semantic_segmentation_layer/segmentation_buffer.hpp b/include/semantic_segmentation_layer/segmentation_buffer.hpp
index 0a21f38..5fd2e5c 100644
--- a/include/semantic_segmentation_layer/segmentation_buffer.hpp
+++ b/include/semantic_segmentation_layer/segmentation_buffer.hpp
@@ -673,7 +673,7 @@ class SegmentationBuffer
      * @param  expected_update_rate How often this buffer is expected to be updated, 0 means there is
      * no limit
      * @param  min_obstacle_height The minimum height of a hitpoint to be considered legal
-     * @param  max_obstacle_height The minimum height of a hitpoint to be considered legal
+     * @param  max_obstacle_height The maximum height of a hitpoint to be considered legal
      * @param  obstacle_max_range The range to which the sensor should be trusted for inserting
      * obstacles
      * @param  obstacle_min_range The range from which the sensor should be trusted for inserting
@@ -767,6 +767,10 @@ class SegmentationBuffer
 
     void setMaxObstacleDistance(double distance) { sq_max_lookahead_distance_ = pow(distance, 2); }
 
+    void setMinObstacleHeight(double height) { min_obstacle_height_ = height; }
+
+    void setMaxObstacleHeight(double height) { max_obstacle_height_ = height; }
+
     /**
      * @brief Set the maximum range (m) to which raytrace clearing trusts the sensor.
      * Points beyond this distance from the sensor origin are not used to clear cells.
@@ -838,6 +842,8 @@ class SegmentationBuffer
     std::recursive_mutex lock_;  ///< @brief A lock for accessing data in callbacks safely
     double sq_max_lookahead_distance_;
     double sq_min_lookahead_distance_;
+    double min_obstacle_height_ = -1e30;  // global-frame z gate; -1e30/1e30 = unbounded until set
+    double max_obstacle_height_ = 1e30;
     tf2::Duration tf_tolerance_;
     
     SegmentationCostMultimap::SharedPtr segmentation_cost_multimap_;
diff --git a/src/segmentation_buffer.cpp b/src/segmentation_buffer.cpp
index f17df90..8dc85b2 100644
--- a/src/segmentation_buffer.cpp
+++ b/src/segmentation_buffer.cpp
@@ -164,6 +164,19 @@ void SegmentationBuffer::bufferSegmentation(
           ++iter_z_global;
           continue;
         }
+        // Height gate (global frame): camera marks ONLY near-ground lane paint;
+        // white objects above the band (tents, barrel tops, signs, people) are
+        // dropped here so they never become marks OR raytrace endpoints. Mirrors
+        // the LiDAR STVL min/max_obstacle_height gate + nav2 ObservationBuffer.
+        // NOTE: two_d_mode pins odom level, so chassis pitch (ramp/bump) shifts a
+        // ground point's z by ~r*sin(pitch); the band is shipped WIDE for that.
+        if (*(iter_z_global) < min_obstacle_height_ || *(iter_z_global) > max_obstacle_height_)
+        {
+          ++iter_x_global;
+          ++iter_y_global;
+          ++iter_z_global;
+          continue;
+        }
         double sq_dist =
           std::pow(*(iter_x_global) - global_origin.point.x, 2) +
           std::pow(*(iter_y_global) - global_origin.point.y, 2) +
diff --git a/src/semantic_segmentation_layer.cpp b/src/semantic_segmentation_layer.cpp
index 0f1bc3e..683ddab 100644
--- a/src/semantic_segmentation_layer.cpp
+++ b/src/semantic_segmentation_layer.cpp
@@ -112,6 +112,8 @@ void SemanticSegmentationLayer::onInitialize()
     declareParameter(source + "." + "class_types", rclcpp::ParameterValue(std::vector<std::string>({})));
     declareParameter(source + "." + "max_obstacle_distance", rclcpp::ParameterValue(5.0));
     declareParameter(source + "." + "min_obstacle_distance", rclcpp::ParameterValue(0.3));
+    declareParameter(source + "." + "min_obstacle_height", rclcpp::ParameterValue(-1e6));
+    declareParameter(source + "." + "max_obstacle_height", rclcpp::ParameterValue(1e6));
     declareParameter(source + "." + "tile_map_decay_time", rclcpp::ParameterValue(5.0));
     declareParameter(source + "." + "visualize_tile_map", rclcpp::ParameterValue(false));
     declareParameter(source + "." + "use_cost_selection", rclcpp::ParameterValue(true));
@@ -129,6 +131,9 @@ void SemanticSegmentationLayer::onInitialize()
     node->get_parameter(name_ + "." + source + "." + "class_types", class_types_string);
     node->get_parameter(name_ + "." + source + "." + "max_obstacle_distance", max_obstacle_distance);
     node->get_parameter(name_ + "." + source + "." + "min_obstacle_distance", min_obstacle_distance);
+    double min_obstacle_height = -1e6, max_obstacle_height = 1e6;
+    node->get_parameter(name_ + "." + source + "." + "min_obstacle_height", min_obstacle_height);
+    node->get_parameter(name_ + "." + source + "." + "max_obstacle_height", max_obstacle_height);
     node->get_parameter(name_ + "." + source + "." + "tile_map_decay_time", tile_map_decay_time);
     node->get_parameter(name_ + "." + source + "." + "visualize_tile_map", visualize_tile_map);
     bool use_cost_selection = true;
@@ -213,6 +218,8 @@ void SemanticSegmentationLayer::onInitialize()
     segmentation_buffer->setClearingEnabled(clearing);
     segmentation_buffer->setRaytraceMaxRange(raytrace_max_range);
     segmentation_buffer->setRaytraceMinRange(raytrace_min_range);
+    segmentation_buffer->setMinObstacleHeight(min_obstacle_height);
+    segmentation_buffer->setMaxObstacleHeight(max_obstacle_height);
     if (clearing) {
       RCLCPP_INFO(logger_,
                   "PR3 raytrace clearing enabled for source %s (raytrace_max=%.2fm, raytrace_min=%.2fm)",
@@ -578,6 +585,18 @@ SemanticSegmentationLayer::dynamicParametersCallback(
               buffer->setMinObstacleDistance(parameter.as_double());
             }
           }
+        } else if (name == name_ + "." + source + "." + "min_obstacle_height") {
+          for (auto & buffer : segmentation_buffers_) {
+            if (buffer->getBufferSource() == source) {
+              buffer->setMinObstacleHeight(parameter.as_double());
+            }
+          }
+        } else if (name == name_ + "." + source + "." + "max_obstacle_height") {
+          for (auto & buffer : segmentation_buffers_) {
+            if (buffer->getBufferSource() == source) {
+              buffer->setMaxObstacleHeight(parameter.as_double());
+            }
+          }
         }
       } else if (type == rclcpp::ParameterType::PARAMETER_INTEGER) {
         for(auto & buffer : segmentation_buffers_) {
